[package]
name = "sentinel-ioc"
version = "0.1.0"
edition = "2021"
description = "Pure IOC extraction and normalization logic for ThreatDeck"
license = "MIT"
repository = "https://github.com/gripebomb/ThreatDeck"
readme = "README.md"

[dependencies]
regex = "1"
serde = { version = "1", features = ["derive"] }
url = "2"
